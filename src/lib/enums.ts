export enum Dir {
	Incomes = 1,
	Expenses = 2
}

export enum GoalType {
    DIFF = 1,
    CATEGORY_GT = 2,
    CATEGORY_LT = 3
}

export enum Frequency {
    OneTime = 1,
    Daily = 2,
    Weekly = 3,
    Monthly = 4,
    Yearly = 5,
}
